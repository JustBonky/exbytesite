{
  "private": true,
  "scripts": {
    "start": "npx parcel src/*.html -p 3000",
    "build": "parcel build src/*.html"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.2.1",
    "parcel": "^2.2.1"
  }
}
