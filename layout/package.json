{
  "private": true,
  "scripts": {
    "start": "npx parcel src/*.html -p 4001",
    "build": "npx parcel build src/*.html"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.2.1",
    "parcel": "^2.2.1"
  }
}
