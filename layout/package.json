{
  "private": true,
  "scripts": {
    "start": "npx parcel src/*.pug -p 5000",
    "build": "npx parcel build src/*.pug",
    "serve": "surge"
  },
  "devDependencies": {
    "@parcel/transformer-pug": "^2.3.1",
    "@parcel/transformer-sass": "^2.2.1",
    "parcel": "^2.2.1"
  },
  "dependencies": {
    "surge": "^0.23.1"
  }
}
